# V2 Integration Tests

These tests exercise multiple V2 components together, particularly persistence and evidence/claim relationships. They ensure that `RunOutcome` translates correctly into the Postgres database schema and that edge cases in persistence boundaries are protected.
