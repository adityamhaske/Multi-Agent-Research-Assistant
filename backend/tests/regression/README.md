# Regression Tests

Regression tests preserve behavior for bugs that previously escaped through production or integration boundaries.

Each test should explicitly state the bug it is protecting against in its header comments. Focused, boundary-specific tests are preferred over full end-to-end setups where possible.
