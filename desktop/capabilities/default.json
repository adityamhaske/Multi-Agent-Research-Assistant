{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The shell carries no plugin permissions — keychain, storage, and networking all live in the sidecar. Least privilege by construction (docs/13 §7).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
